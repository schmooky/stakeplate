{
  "name": "stakeplate",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.32.1",
  "description": "Monorepo for @stakeplate/core — a batteries-included Stake Engine game core (RGS transport, boot, round FSM, HUD binding, audio, i18n, compliant rules) — plus create-stakeplate, the rules editor, a local RGS emulator, and the basic-slot example.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter basic-slot dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "example:dev": "pnpm --filter basic-slot dev",
    "example:build": "pnpm --filter basic-slot build",
    "rgs:dev": "pnpm --filter rgs-emulator dev"
  }
}
